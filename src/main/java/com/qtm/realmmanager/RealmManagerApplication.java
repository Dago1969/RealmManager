package com.qtm.realmmanager;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Classe di bootstrap per avvio Spring Boot di RealmManager.
 */
@SpringBootApplication
public class RealmManagerApplication {
    public static void main(String[] args) {
        SpringApplication.run(RealmManagerApplication.class, args);
    }
}
